<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MVVM.Pages.WelcomePage"
             BackgroundColor="{StaticResource mainColor}"
             BindingContext="{Binding Main, Source={StaticResource Locator}}"
             Title="Mis Pedidos.">

    <StackLayout Padding="20" VerticalOptions="Center" HorizontalOptions="Center">
        <Label TextColor="{StaticResource MenufontColor}" FontSize="20" Text="Bienvenid@s"></Label>
        <Button Command="{Binding StartCommand}"
                HeightRequest="40"
                BorderRadius="20"
                BackgroundColor="{StaticResource menuBackgroudColor}"
                TextColor="{StaticResource MenufontColor}"
                Text="Inicial."></Button>

    </StackLayout>

</ContentPage>